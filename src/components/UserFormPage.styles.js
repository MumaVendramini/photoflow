// src/components/UserFormPage.styles.js
import styled from 'styled-components';
import theme from '../themes/theme';

export const PageContainer = styled.div`
  max-width: 600px;
  margin: 40px auto;
  padding: 32px;
  background-color: ${theme.colors.surface};
  border-radius: 16px;
`;
